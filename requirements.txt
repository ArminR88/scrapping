selenium>=4.15.0
playwright>=1.39.0
webdriver-manager>=4.0.0
pandas>=2.1.3
# Optional utilities you may already want for experiments
requests>=2.31.0
httpx>=0.25.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
cloudscraper>=1.2.68

# Notes:
# - After installing 'playwright', run: `python -m playwright install` (the Dockerfile already runs playwright install).
# - webdriver-manager will auto-download chromedriver when running Selenium if chromedriver isn't present system-wide.